spring:
  application.name: passwordService
#  jackson.mapper:
#    com.dcai.passwordService.model.User: userMapper
  jackson.serialization.indent_output: true
    
server:
  port: 8100
  ssl:
    key-password: password
    key-store:  classpath:selfsigned.jks
    key-store-type: JKS
  error.whitelabel.enabled: true
    
security:
  basic:
    enabled: true
  oauth2:
    client:
      clientId: 313119502609478
      clientSecret: 475a3ada69c671ec13debf132c76f416
      accessTokenUri: https://graph.facebook.com/oauth/access_token
      userAuthorizationUri: https://www.facebook.com/dialog/oauth
      tokenName: oauth_token
      authenticationScheme: query
      clientAuthenticationScheme: form
    resource:
      userInfoUri: https://graph.facebook.com/me

# file:
#  password: <path to your passwd file, default to /etc/passwd>
#    field:
#      delimiter: <default to ':'>
#    commentMarker: <default to hash sign>
#  groups: <path to your groups file, default to /etc/groups>
#    field:
#      delimiter: <default to ':'>
#    commentMarker: <default to hash sign>